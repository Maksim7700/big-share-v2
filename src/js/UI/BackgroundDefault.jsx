const BackgroundDefault = ({children}) => {
    return (
        <>
            <div style={{backgroundColor: '#101A24'}}>
                {children}
            </div>
        </>
    )
}

export default BackgroundDefault;